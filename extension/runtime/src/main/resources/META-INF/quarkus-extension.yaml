name: "Temporal"
description: "Temporal is a developer-first, open source platform that ensures the successful execution of services and applications."
artifact: ${project.groupId}:${project.artifactId}:${project.version}
metadata:
  short-name: "temporal"
  keywords:
    - "workflow"
    - "orchestration"
    - "cron"
    - "state-machine"
  categories:
    - "integration"
  config:
    - "quarkus.temporal."
  status: "experimental"
  guide: "https://quarkiverse.github.io/quarkiverse-docs/quarkus-temporal/dev/index.html"
  icon-url: "https://raw.githubusercontent.com/quarkiverse/quarkus-temporal/main/docs/modules/ROOT/assets/images/temporal_logo.svg"
  lib-ga: io.temporal:temporal-sdk[https://temporal.io]